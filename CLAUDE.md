# chux.dev

## Node version

Always use the Node version defined in `.nvmrc` before running any Node/npm/Gatsby commands, e.g.:

```
export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && nvm use
```
